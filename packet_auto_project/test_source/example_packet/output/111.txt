
from pwn import *

p = remote("10.12.0.1", 1234)
        
p.recv("""You can only access this service on your own host! It does not have a flag so there's no point in attacking it anyway""")

p.close()
